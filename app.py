import streamlit as st

st.set_page_config(page_title='Watchlist Behavior Monitor', layout='wide')
st.title('Watchlist Behavior Monitor')
st.write('Run pipeline with: `python -m src.run_daily` then use pages in sidebar.')
